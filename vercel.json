{
    "builds": [
      {
        "src": "app.py",
        "use": "@vercel/python",
        "config": {
          "buildCommand": "pip install setuptools && pip install -r requirements.txt"
        }
      }
    ]
  }
  