{
    "builds": [
      {
        "src": "your_entry_point.py",
        "use": "@vercel/python",
        "config": {
          "runtime": "python3.12"
        }
      }
    ],
    "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt"
  }